# 新学期语数英赛事一站通

这是从 WorkBuddy 导出的无依赖静态网页，可直接使用 GitHub Pages 发布。

## 发布

在 GitHub 仓库的 **Settings → Pages** 中，选择 **Deploy from a branch**、`main` 分支、`/(root)` 目录，保存后等待发布完成。

发布地址通常为：`https://<GitHub 用户名>.github.io/<仓库名>/`
